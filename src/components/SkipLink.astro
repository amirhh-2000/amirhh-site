---
import { ui } from "@/i18n";
import type { Locale } from "@/types";

const { locale } = Astro.props as { locale: Locale };
---
<a class="sr-only focus:not-sr-only focus:fixed focus:start-1 focus:top-1.5" href="#main">{ui[locale].skip}</a>
